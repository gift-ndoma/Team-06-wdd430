export type UserSlim = {
  id: number;
  email: string;
  password: string;
};

export type User = {
  id: number;
  email: string;
  name: string;
  address: string;
  account_level: string;
  artisan_id: string;
};